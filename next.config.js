/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	env: {},
};

module.exports = nextConfig;

module.exports = {
	images: {
		domains: ['favoland-product-image.s3.ca-central-1.amazonaws.com'],
	},
};